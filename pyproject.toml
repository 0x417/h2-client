[project]
name = "hyper2-client"
version = "0.1.0"
description = "Thin client for a hyper2 optimisation server. Standard library only."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
